package com.dmitrikramar.gamelibrary.entity;

/**
 * Enum representing the possible roles that can be assigned to users in the system.
 */
public enum RoleName {
    /** Standard user with limited permissions. */
    USER,

    /** Administrator with elevated permissions. */
    ADMIN
}
